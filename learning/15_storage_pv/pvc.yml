kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: demo-pvc
  namespace: default

spec:
  storageClassName: ""
  
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 2Gi
